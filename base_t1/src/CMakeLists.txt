
MESSAGE(STATUS "This is table PATH " ${EXECUTABLE_OUTPUT_PATH})
SET(SRC_LIST main.c)
SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)
ADD_EXECUTABLE(hello ${SRC_LIST})
INSTALL(TARGETS hello
    RUNTIME DESTINATION bin
    )
